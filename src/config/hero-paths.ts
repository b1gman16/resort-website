// Pages with a full-bleed hero the navbar should float transparently
// over. Shared between Navbar and NavSpacer so they never fall out of
// sync with each other.
export const FULL_BLEED_HERO_PATHS = ["/", "/rooms"];